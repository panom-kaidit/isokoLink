version: "3.9"

services:
  app:
    build: .
    container_name: isokolink
    restart: always
    network_mode: host        # uses the server's network directly 
    env_file:
      - .env                  # reads the .env file at the project root